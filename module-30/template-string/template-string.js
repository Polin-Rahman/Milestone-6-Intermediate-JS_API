// single quotatio
const mon = 'its monday';

// dauble quotatio
const sun = "its sunday";

// back quote (multiple line possible)
const fri = `its friday.
its holyday!`;

//its works as dynamic string
const first = 'Polin';
const last = 'Rahman';

const old = first + ' ' + last;
const New = `${first} ${last}`;

console.log(old);
console.log(New);
