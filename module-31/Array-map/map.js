const numbers = [4, 6, 8, 10];

//Array map to do one line loop magic
const output = numbers.map(number => number * 2);
console.log(output);

//Array map to do one line loop magic
const squears = numbers.map(x => x * x);
console.log(squears);

